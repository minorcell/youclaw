---
summary: "首次引导脚本"
read_when:
  - 新工作区初始化
---

请按顺序完成：

1. 阅读 `AGENTS.md` 与 `SOUL.md`
2. 完成 `IDENTITY.md` 的基础信息
3. 与用户确认并填写 `USER.md` 关键字段
4. 把当前环境中会影响执行效率的信息写入 `TOOLS.md`
5. 确认 `MEMORY.md` 与 `memory/` 目录可用于后续沉淀

完成后删除 `BOOTSTRAP.md`。
